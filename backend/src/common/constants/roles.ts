export enum AccountType {
  READER = 'reader',
  AUTHOR = 'author',
  ADMIN = 'admin',
  WRITER = 'writer',
}

export enum AdminRole {
  SUPER_ADMIN = 'super_admin',
  MODERATOR = 'moderator',
  FINANCE = 'finance',
}
